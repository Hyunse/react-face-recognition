import React, { Component } from 'react';
import styled from 'styled-components';

const Container = styled.div`
  background-color: #dfe6e9;
`;

class Main extends Component {
  public render() {
    return <Container>Camera</Container>;
  }
}

export default Main;
